---
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  # firewalld, selinux, sysctl modules used by k8s_common.
  - name: ansible.posix
    version: ">=1.5.0"
  # modprobe module used by k8s_common.
  - name: community.general
    version: ">=8.0.0"
