---
- name: "Step 01 - Preflight checks (OS, memory, FQDN, time sync)"
  ansible.builtin.import_tasks: 01_preflight.yml
  tags:
    - step01
    - preflight

- name: "Step 02 - Configure the OLVM 4.5 repositories"
  ansible.builtin.import_tasks: 02_repositories.yml
  tags:
    - step02
    - repos

- name: "Step 03 - Install the ovirt-engine packages"
  ansible.builtin.import_tasks: 03_install_engine.yml
  tags:
    - step03
    - install

- name: "Step 04 - Run engine-setup unattended from the answer file"
  ansible.builtin.import_tasks: 04_engine_setup.yml
  tags:
    - step04
    - setup

- name: "Step 05 - Verify the Engine and show access URLs"
  ansible.builtin.import_tasks: 05_verify.yml
  tags:
    - step05
    - verify
